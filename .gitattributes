# 在 Windows 上編輯過的檔案常被改成 CRLF，git 會把整個檔案都當成變動
# （曾經一次跑出 6269 行的假差異，實際內容一個字都沒改）。
# 統一以 LF 存進版本庫；只有 .bat 必須維持 CRLF，否則 Windows 不一定跑得動。
* text=auto eol=lf
*.bat text eol=crlf

# 二進位檔不要做任何換行轉換
*.png binary
*.jpg binary
*.ico binary
*.bundle binary
